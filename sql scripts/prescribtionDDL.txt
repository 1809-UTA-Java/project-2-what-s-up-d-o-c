DROP TABLE PRESCRIPTIONS;
DROP SEQUENCE PRESCRIPTIONGEN;

CREATE TABLE PRESCRIPTIONS (
	PRESCRIPTION_ID NUMBER(*,0) PRIMARY KEY,
	DOCTOR_ID NUMBER(*,0),
	PATIENT_ID NUMBER(*,0),
	DRUG_ID NUMBER(*,0),
	AMMOUNT NUMBER(*,0)
);

CREATE SEQUENCE PRESCRIPTIONGEN START WITH 1 INCREMENT BY 1 NOCACHE;